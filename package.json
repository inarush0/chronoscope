{
  "name": "chronoscope",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite dev --open",
    "build": "vite build",
    "build:binary": "npm run build && go build -o chronoscope .",
    "build:linux": "npm run build && GOOS=linux GOARCH=amd64 go build -o chronoscope-linux-amd64 .",
    "preview": "vite preview",
    "check": "tsc -p tsconfig.json",
    "test": "vitest run",
    "coverage": "vitest run --coverage",
    "check:tests": "tsc -p tsconfig.tests.json",
    "format": "prettier --write \"src/**/*.{ts,css}\" \"dataset/**/*.ts\" package.json tsconfig.json vite.config.ts vitest.config.ts",
    "format:check": "prettier --check \"src/**/*.{ts,css}\" \"dataset/**/*.ts\" package.json tsconfig.json vite.config.ts vitest.config.ts",
    "check:dataset": "tsc -p dataset/tsconfig.json",
    "build-db": "node dataset/build.ts",
    "validate": "node dataset/validate.ts",
    "check:artifact": "node dataset/check-artifact.ts"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@vitest/coverage-v8": "4.1.11",
    "prettier": "^3.8.1",
    "typescript": "^5.9.3",
    "vite": "^7.3.1",
    "vitest": "4.1.11"
  },
  "dependencies": {
    "pixi.js": "^8.16.0"
  }
}
